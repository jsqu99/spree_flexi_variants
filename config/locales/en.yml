---
en:
  configure_option_type: Configure option type for use in a dynamic variant
  configure_dynamic: Option type %{option_type} is currently globally configured by default as shown below.  Modify if desired
  configure_dynamic_for_product: Option values for %{product}:%{option_type} are currently set up as shown below.  Further configure if desired

