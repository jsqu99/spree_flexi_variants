  <% session_key = Rails.application.config.session_options[:key] %>

<%# I just need a form tag so I can call jquery.serialize %>
<%= form_tag '/ignore', {:id => 'variant_configurations', :multipart => true} do %>
  <%# right now, we don't have any ad-hoc options, but this _should_ work out of the box if we add them %>
  <%= render '/spree/shared/variant_configurations/ad_hoc_option_types' %>

  <%= hidden_field_tag :session_id, u(cookies[session_key]), :name => session_key %>

  <%= render '/spree/shared/variant_configurations/customizations' %>

<% end %>